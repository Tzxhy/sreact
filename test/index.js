import React from 'src/index';

class App extends React.Component {

    render() {
        return (<div>test</div>);
    }
}

/* eslint-disable */
React.render(<App />, document.getElement('root'));
